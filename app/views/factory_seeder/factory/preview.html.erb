<div class="container">
    <div class="header">
        <%= link_to "← Back to Factory", factory_path(@factory_name), class: "back-link" %>
        <h1>🔍 Preview: <%= @factory_name %></h1>
    </div>
    
    <div class="content">
        <div class="preview-section">
            <h3>Preview Data</h3>
            <p>This is what would be generated with the current settings:</p>
            
            <div class="preview-data">
                <pre><%= JSON.pretty_generate(@preview_data) %></pre>
            </div>
            
            <div class="preview-actions">
                <%= link_to "← Back to Factory", factory_path(@factory_name), class: "btn btn-secondary" %>
                <%= link_to "Generate Seeds", factory_path(@factory_name), class: "btn btn-primary" %>
            </div>
        </div>
    </div>
</div>
